function PagePrincipalComponent(props) {
    var principal = <div>
    <HeaderComponent/>
    <SearchBlogComponent/>
    <BlogListComponent/>
    <FooterComponent/>
</div>

    //RETURN LE TABLEAU
    return principal;
}